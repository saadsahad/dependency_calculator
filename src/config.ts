/** Central place for links/config that will need real values before launch. */
export const SITE_CONFIG = {
  /** Where the result screen's primary CTA points. Replace with the real booking link. */
  bookingUrl: "https://cal.com/nativize/working-session",
  brandName: "Nativize",
};
