// Scoring logic is shared between the client and the server (the server
// recomputes the score from submitted answers rather than trusting the
// client-calculated value). See ../../shared/scoring.ts for the source.
export * from "../../shared/scoring";
