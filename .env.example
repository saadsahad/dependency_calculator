# Port the Express API server listens on (dev default is 3001)
PORT=3001

# Where the SQLite database file is stored (defaults to ./data/leads.sqlite)
# DATABASE_PATH=./data/leads.sqlite

# Set this to view captured leads at GET /api/leads with
# `Authorization: Bearer <token>`. Leave unset to disable the endpoint.
# ADMIN_TOKEN=change-me
